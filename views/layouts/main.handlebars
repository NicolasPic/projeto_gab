<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        {{#if customHeaderHome}}
            {{> headerHome}}  <!-- Cabeçalho principal -->
        {{/if}}

        {{#if customHeaderLogin}}
            {{> headerLogin}}  <!-- Cabeçalho de login -->
        {{/if}}

    </header>

    <main>
        {{{body}}}  <!-- O conteúdo da página será injetado aqui -->
    </main>

     {{> footer}}<!-- Rodapé comum para todas as páginas -->
</body>
</html>
